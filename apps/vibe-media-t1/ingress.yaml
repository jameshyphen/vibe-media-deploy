apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vibe-media-t1
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: vibe-t1.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vibe-media-t1
                port:
                  number: 80
