apiVersion: v1
kind: Service
metadata:
  name: vibe-media-t2
spec:
  selector:
    app: vibe-media-t2
  ports:
    - port: 80        # cluster‑internal port
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
